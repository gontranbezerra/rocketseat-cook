import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    height: 58,
  },
  ingredientsContent: {
    flexWrap: 'wrap',
    flexDirection: 'row',
    paddingBottom: 200,
    gap: 12,
    paddingHorizontal: 32,
  },
});
